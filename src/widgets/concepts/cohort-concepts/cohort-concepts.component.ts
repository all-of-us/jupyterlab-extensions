import {Component} from '@angular/core';

import * as htmlTemplate from 'html-loader!./cohort-concepts.component.html';
const template = '' + htmlTemplate;

@Component({
  selector: 'cohort-concepts',
  template: template
})
export class CohortConceptsComponent {
}
